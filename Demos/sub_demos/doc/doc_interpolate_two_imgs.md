# Interpolate two images
The first one gives structure (coarse scales) while the second one gives texture (fine scales).

> Play with renoise ratio r to control strenght of stylization .

## Parameters

nc : 16 or 32 correspond respectively to the 1M and 4M parameters models.

S : Number of sampling steps.

r : Renoising time ratio.

size : Size of the output.